{
  "name": "nodalali",
  "version": "1.0.0",
  "description": "Tanzania house hunting without dalali — direct landlord to tenant",
  "scripts": {
    "start": "node server.js",
    "dev": "npx serve -s . -l 3000",
    "build": "mkdir -p www && cp index.html style.css script.js manifest.json firebase-config.js icon.svg sw.js www/ && npx cap sync android",
    "open:android": "npx cap open android",
    "apk": "npm run build && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.1",
    "@capacitor/cli": "^8.3.1",
    "@capacitor/core": "^8.3.1",
    "@supabase/supabase-js": "^2.105.1",
    "bcryptjs": "^3.0.3",
    "cookie-parser": "^1.4.7",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.4.1",
    "firebase-admin": "^13.8.0",
    "jsonwebtoken": "^9.0.3",
    "node-fetch": "^2.7.0",
    "serve": "^14.2.3"
  }
}
